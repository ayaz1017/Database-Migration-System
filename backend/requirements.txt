# ──────────────────────────────────────────
# Fluxline Backend — Direct Dependencies
# Pinned to exact versions for reproducible builds.
# Last audited: 2026-07-19
# Removed: pywin32 (Windows pip-freeze artifact, zero imports)
#          async-lru (zero imports in codebase)
#          httpx (test-only; moved to requirements-dev.txt)
# ──────────────────────────────────────────

# Web Framework
fastapi==0.137.1
uvicorn[standard]==0.49.0
python-multipart==0.0.32
starlette==1.3.1
slowapi==0.1.10

# Data Validation
pydantic==2.13.4
pydantic-settings==2.14.1
python-dotenv==1.2.2

# Authentication
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# Database Connectors
pyodbc==5.3.0
mysql-connector-python==9.7.0
psycopg2-binary==2.9.12
oracledb==4.0.1

# AI / LLM
ollama==0.6.2

# Graph / Schema Analysis
networkx==3.6.1

# Utilities
requests==2.34.2
structlog==26.1.0
websockets==15.0.1
faker==33.3.1
httpx==0.28.1
apscheduler==3.11.0

